x <- 9                         => x를 9로 선언합니다

if <- 3 <= x                   => 만약 x가 3보다 크거나 같다면
  con <- 'x is more than 3'    => 'x is more than 3'를 출력합니다
  exit <- 0                    => 0으로 끝냅니다
->                             => if문을 끝냅니다